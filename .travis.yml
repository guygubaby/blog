language: node_js

node_js:
  - "10"

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"

cache: yarn

install:
  - yarn

script:
  - ./deploy.sh

notifications:
  email: 
    recipients:
      - 1907004005@qq.com
    on_success: change
    on_failure: always
